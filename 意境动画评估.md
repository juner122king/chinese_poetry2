# 意境动画系统 —— 客观评估与优化方案


## Context

需求：盘点项目内**所有**意境动画效果，逐项给出评价、不足与优化方案，要求客观。

本文档是**评估报告 + 分级实施方案**。评估基线不是主观审美，而是三条可验证的标准：

1. **项目自订规则**：`README.md:76-79` 的「主题签名原则」——每主题一个主运动、避免「光斑+粒子」双叠、雾分 soft/heavy/关，并点名约定「华灯 = 仅灯笼 · 寒江 = 江线+雪粒子（无 CSS 雪点）」。凡实现与此冲突，记为缺陷而非风格差异。
2. **尺度自洽**：同一套场景被复用到全视口 Hero（~900px）与卡片（108–260px）两种画布，运动幅度、元素尺寸、粒子像素大小必须随画布缩放。
3. **性能与无障碍**：常驻合成层数量、每帧 CPU 开销、WebGL 上下文生命周期、`prefers-reduced-motion` 下是否仍保留「静态形态」。

读过的关键文件：`components/InkBackground.tsx`(1119行) · `ParticleBackground.tsx`(408行) · `CelestialBodies.tsx`(630行) · `PoemCardAtmosphere.tsx` · `HeroSection.tsx` · `ThemeScene.tsx` · `ImageryBar.tsx` · `HomeImageryGates.tsx` · `PoemCard.tsx` · `Banxin.tsx` · `ScrollReveal.tsx` · `lib/theme-map.ts` · `lib/use-hover-focus-active.ts` · `app/globals.css`(1951行)。

**结论摘要**：设计层的品味与纪律是这套系统最强的部分——主题签名有明确取舍、水墨语汇克制、无一处廉价特效。缺陷集中在**工程实现**：4 处功能性 bug、1 处 README 与代码矛盾、一整类「Hero 场景直接搬进卡片」导致的尺度失配、以及可量化的性能与无障碍退化。以下逐项列出。

---

## 一、评估对象清单

### 三个宿主（同一套场景，四种画布）

| 宿主 | 文件 | 画布 | intensity | 主题 |
|---|---|---|---|---|
| Hero 首屏 | `HeroSection.tsx` | 全视口（`.hero-scene` inset -5%/-2%） | `full` | 随机诗作 theme |
| 详情/作者页底 | `ThemeScene.tsx` | `fixed inset-0` | `full` | 该诗 theme |
| 列表页底 | `app/{page,imagery,authors,shelf,poems}/page.tsx` | 全视口 | `soft` | 固定 `landscape` |
| **卡片 hover** | `PoemCardAtmosphere.tsx` | **108px / 208px / ~240px** | `soft` | 卡片 theme |

`intensity="soft"` 的实际作用只有两条（`InkBackground.tsx:334,344`）：`size = 0.7`（仅传给日月）与 `atmScale = 0.72`（乘在若干层 opacity 上）。**没有任何几何/位移的缩放**——这是第二章大量「尺度」类缺陷的共同根因。

### 效果清单

- **CSS/SVG 氛围（16 项）**：渐变底 + 光斑、云、雾、日月、山峦、地平/水际、雨、CSS 雪点、灯笼、禽鸟、涟漪、酒焰、竹木、田埂、舟影、暗角/纸纹
- **WebGL 粒子（6 模式）**：`stars` / `stars-frontier` / `firefly` / `petals` / `leaves` / `snow`（22 主题中 10 个为 `none`）
- **编排/交互（4 项）**：Hero 入场编排 `HERO_CHOREO`、卡片 hover 氛围、意境带两速过渡、`ScrollReveal`/`PageTransition`/`Banxin` 落版

---

## 二、逐效果评价

### A. CSS / SVG 氛围层

#### A1 渐变底 + 光斑（`InkBackground.tsx:389`, `:195-205`）
- **评价**：22 主题各自手写 `gradient`，色阶收在 `#0a1018 → #1a1a1a` 一类低明度区间，底色是整套意境辨识度的主要来源，做得扎实。叠加的两层 `radial-gradient` 光斑用 `visual.glow` 串联主题色，成本近乎为零。
- **不足**：`className="... transition-[background] duration-1000"` 是**死代码**——CSS 无法插值 `background-image` 中的 gradient，且 `theme` 在任何宿主里都不会原地变更（卡片是整体 remount）。这行注释性的「1 秒主题淡入」从未发生。
- **优化**：删除该 transition；若确实想要主题交叉淡入，改成两层 gradient 叠放 + `opacity` 互切（唯一能被 GPU 插值的方案）。

#### A2 云（`globals.css:311-343`；`InkBackground.tsx:410,420,656,667`）
- **评价**：`cloudDrift` 52s / 68s reverse，位移仅 ±2.2%，且刻意不做 scale/opacity 脉冲——注释「水墨空蒙，非呼吸灯」与实现一致。`high`/`band`/`sea` 三形与山形错位分层，`blur(30-34px)`，观感干净。
- **不足**：`cloud-a`/`cloud-b` 只有两个相位；`sea` 形下两层同时存在时靠 52s vs 68s 拉开，尚可，但 `high` 形只有单层在动。
- **优化**：低优先级。给 `-b` 加 `animation-delay: -18s` 让初始相位错开（现在两层都从 0% 起，首 10 秒内同向运动）。

#### A3 雾（`globals.css:301-331`；`InkBackground.tsx:696,711,726`）
- **评价**：山腰岚 + 谷底雾两条水平带，36s/48s reverse，`blur(36-40px)`，`borderRadius:100%` 让边缘自然消失。soft/heavy 分级真实生效（opacity 0.22→0.38、高度 16%→22%、heavy 额外加一张 `.ink-mist-sheet`）。这是覆盖面最广、最可靠的氛围手段。
- **不足**：**第三条雾复用了 `.ink-mist-a`**（`:726`，在 `heavy` 或 `intensity==="full"` 时渲染），与山腰那条同为 `mistDrift 36s` 且无 `animation-delay` → 两条雾**完全同相位同步摆动**，视觉上等于一条更宽的雾，白付出一层合成开销却拿不到层次错动。
- **优化**：新增 `.ink-mist-c { animation: mistDrift 41s ease-in-out infinite; animation-delay: -13s; }`，或最省事地给 `:726` 那层加 `style={{ animationDelay: "-17s" }}`。

#### A4 日月（`CelestialBodies.tsx`，630行）
- **评价**：整套系统里最讲究的一件。7 种月相（满/残/新/水中/云遮…）+ 3 种日相，每颗月由外晕/中晕/实心盘/明暗界线（`mixBlendMode:"multiply"`）四层构成，可选水面倒影；只有外晕做 `celestialBreathe` 呼吸（14s/18s，opacity 0.55↔0.9），月盘本体保持稳定——这个取舍非常正确，避免了「呼吸灯」廉价感。SVG id 用 `useId()`，无多实例冲突。
- **不足**（三点，均可验证）：
  1. **mount 后跳位**：`useEffect(() => setJitter(sampleMoonJitter(variant)))`（`:385-403`）在首帧之后才采样位置/大小/模糊，首帧用 `midPlacement` 的中间值 → **月亮和太阳在页面稳定后会可见地跳一次**。同一首诗每次访问构图还会变，破坏「这首诗有它固定的画面」。
  2. **卡片内比例失控**：`sizePx = 120 * scale * base.size * jitter`（`:415`），`scale=0.7`、`base.size` 0.72–1 → 实际直径 **60–84px**。在 900px 高的 Hero 里占 7–11% 高度（正确）；在 `.imagery-bar`（`min-height: 6.75rem = 108px`）里占 **56–78% 高度**，外晕还要更大 —— 卡片里等于一颗糊满整格的白团。
  3. `theme` 是死参数（`void _theme`）。
- **优化**：见 P0-2（确定性种子）与 P1-1（尺度）。`sizePx` 改为 `min(120*scale*..., 24cqh)`，卡片容器声明 `container-type: size`。

#### A5 山峦（`InkBackground.tsx` `MOUNTAIN_FORMS`）
- **评价**：5 形（distant/rolling/peaks/jagged/range）× 远/中/近三层 SVG path，远景 `blur(2.8-4px)`、`footHaze` 山脚雾——空气透视处理是对的，比单纯降 opacity 高级。静态，不参与动画，符合「一主一辅」纪律。
- **不足**：三层全屏 SVG + filter blur 在移动端是实打实的 raster 成本；卡片内 heightClass 36–52% 的山脊在 108px 高的格子里被压成一条锯齿噪线。
- **优化**：卡片 scope 下只渲染远景一层（或整体关闭，见 P1-2）。全屏场景可把远景 blur 预烘进 path 的渐变 fill，省掉一次 filter。

#### A6 地平 / 水际（`HorizonAtmosphere`，`InkBackground.tsx:512-518`）
- **评价**：plain（尘岚）/ water（水光）/ frost（霜气）三形，接受 `intensityScale`，注释明确「不是一条白杠」——克制得当，是 `parting`/`frontier` 这类无山无月主题的主要空间感来源。
- **不足**：静态。`parting`（离别）全靠它 + 高云 + 淡山，是六道首页入口之一，hover 后几乎没有可感知的运动回报。
- **优化**：见 P2-1（给静态主题补低成本签名运动）。

#### A7 雨（`InkBackground.tsx:740-830`；`globals.css:600-680`）
- **评价**：58 条手写雨滴元组，含 far/near 双景深、统一风向 `--rain-tilt: 4deg`（`rainDropFall` 用 `sin/cos` 让路径与倾角一致，数学上正确）、`.rain-veil` 空蒙罩、`.rain-field` 椭圆 mask 护住中央文字，还刻意在 left 40–60% 留了稀疏带。工程上是全项目最细的一处。
- **不足**：
  1. 注释写「弃硬线，改软水痕点滴」（`globals.css:598`），实现仍是 `width: 1px` × 18–26px 的渐变竖线（`far` 加 `blur(0.35px)`）——**是细线，不是点滴**，注释与实现不符。
  2. `--rain-fall: 120vh` 是**视口单位**。在 208px 卡片里雨滴 5.6s 走完 1080px 行程，只有约 1/5 时间落在卡内 → 卡片里的雨是「一闪而过 + 长时间空窗」的断续感，不是连绵烟雨。
  3. 58 条硬编码元组难维护；中央稀疏带与 mask 中央透明区**双重留空**，窄屏下中间会明显缺雨。
  4. 58 个元素常驻 `will-change: transform, opacity`（`:646`）。
- **优化**：改程序化生成（确定性种子 + 按容器宽度分布）；行程单位换成 `cqh`/百分比；`will-change` 去掉（transform/opacity 本身已走合成）；中央稀疏带与 mask 二选一。

#### A8 CSS 雪点（`InkBackground.tsx:827-851`）
- **评价**：地面雪洗 + 8 颗 `radial-gradient` 亮点，成本极低。
- **不足**：**与 README 明文冲突**。`README.md:79` 写「寒江 = 江线+雪粒子（无 CSS 雪点）」，但 `theme-map.ts:171-178` 的 `snow-river` 同时是 `snow: true` 和 `particles: "snow"` → 8 颗**静止**的雪点叠在飘落的 WebGL 雪上，静止点在动态雪里格外显眼（露馅）。
- **优化**：`snow-river` 改 `snow: false`（保留 `winter` 的 `snow: true`，README 只点名了寒江）。

#### A9 灯笼（`globals.css:446-597`）
- **评价**：三条独立轨迹 A/B/C，40–56s 极慢升起、横向 3→20px 漂移、scale 0.76→0.4 透视缩小、后 45% 时间轴连续熄灭、`fill-mode: both` + `linear` 保证延迟期间不留静态亮点。这是全项目**关键帧写得最认真**的一段，`festival` 的唯一主运动，与「一主一辅」原则严格一致。
- **不足**：
  1. **mount 后爆发**：`lanternList = lanterns ?? [两个硬编码默认]`，`useEffect` 后 `sampleLanterns()` 给出 8–11 盏 → 首帧 2 盏、随后**突然变成 8–11 盏**。
  2. 关键帧位移全是 `vh`：`-54vh` 在 208px 卡片里 ≈ -486px，灯笼约走到时间轴 **45%（-26vh ≈ -234px）就已升出卡外**，后 55% 的行程和整段熄灭都发生在可视区外 → 卡片里看到的是「灯笼从下方冒出后很快消失」，不是「华灯缓升」。
  3. 8–11 个元素常驻 `will-change: transform, opacity`。
  4. **reduced-motion 下被整体隐藏**（见 D1），而 `festival` 是 `mist:false` + `particles:"none"` → 退化为纯渐变，主题彻底消失。
- **优化**：确定性种子（P0-2）；位移单位换 `cqh` 或由 `--scene-h` 变量驱动（P1-2）；reduced-motion 保留静态灯笼（P0-5）。

#### A10 禽鸟（`globals.css:395-437`；`InkBackground.tsx`）
- **评价**：7 只远景雁阵 + 近景一对，`birdsFlockDrift 26s`（±12px）/`birdsNearDrift 32s` 横移，`birdsBob 4.5s` 上下浮。分层与速度差是对的。
- **不足**：**没有扇翅**。鸟形是固定 path，只做整体平移+浮动 → 观感是「贴纸在飘」，而非在飞。这是本项目里唯一「运动与物理直觉相悖」的效果。
- **优化**：给翅膀两组 path 做 4–5 帧 `opacity` 交替（或 `d` 值 morph），周期 0.5–0.8s、各鸟错相位；成本 = 一个额外 keyframe。

#### A11 涟漪（`InkBackground.tsx:988-1000`；`globals.css:685,706-716`）
- **评价**：思路对（水面同心圈慢脉动，`ease-out`，无硬边闪烁）。
- **不足**：**两个功能性缺陷**。
  1. **三环不同心**：`className="... left-1/2 top-0"` 三环共用 `top: 0`，但 `height` 分别是 12/19/26px、`width` 是 48%/76%/104% → 三环的**垂直中心互不重合**，呈上边缘对齐的嵌套椭圆，不是同心涟漪。
  2. **原地脉动而非扩散**：`ripplePulse` 是 `scale 1→1.06` + `opacity .2→.45`，幅度 6% 且回弹 —— 涟漪的物理本质是「向外扩张同时衰减」，现在读起来是三个圈在轻微呼吸。
- **优化**：三环改为共享中心（同一 `top` 基线 + `translate(-50%,-50%)`，或统一 `aspect-ratio` 让 height 由 width 推出）；`ripplePulse` 改单向：`scale 0.85→1.55` + `opacity .45→0`，三环 `delay` 各差 1/3 周期，形成连续外扩。

#### A12 酒焰（`globals.css:690-705`）
- **评价**：`wine` 主题 `mist:false`/`mountains:"none"`/`particles:"none"`，全靠 `.wine-ember` 一个 7.5s 暖焰脉动（opacity .72→1、scale 1→1.06）+ `blur(2px)`。**这是「一主一辅」执行得最纯粹的一例**，并且 reduced-motion 下保留静态暖晕（`globals.css:745-750`）—— 这个降级模式是全项目应当推广的正确范式。
- **不足**：尺寸 `min(42vh,320px)` × `min(48vw,360px)` 又是视口单位，在 108px 卡片里光核直径远超卡片本身，卡片变成一块均匀橙色。
- **优化**：仅尺度问题，随 P1-2 一并处理。

#### A13-A15 竹木 / 田埂 / 舟影
- **评价**：都是静态 SVG。竹木 5 竿 + 8 片模糊叶椭圆，田埂用软墨带而非硬脊线，舟影 `min(11vw,88px)` 且横向位置随机 —— 造型都好，克制。
- **不足**：三者**完全静态**。`bamboo`/`landscape` 一类主题因此只剩雾在飘。舟影位置同样是 mount 后随机（`setBoatLeft(rand(18,48))`，`InkBackground.tsx:360-363`）→ 跳位。
- **优化**：竹木加一条极慢的 `transform: rotate(±0.4deg)` 竿身摆（12s+），叶片错相位——单个 keyframe 即可让「竹木」成立。舟影随种子固定。

#### A16 暗角 / 纸纹（`InkBackground.tsx:1114`；`globals.css` `.noise-overlay::after`）
- **评价**：`radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.55) 100%)` 收束视线，纸纹用 `mix-blend-mode: overlay` 且 z-index 低于粒子（`z-[2]`）—— 层序正确。
- **不足**：暗角**不乘 `atmScale`**，卡片里 0.55 的四角黑压 + `PoemCardAtmosphere` 的 `bg-ink/25` scrim 叠加，208px 卡片四角明显发黑，把本来该「透出意境」的 hover 压成了灰卡。
- **优化**：`0.55 * atmScale`，或卡片 scope 下降到 0.28。

---

### B. WebGL 粒子层（`ParticleBackground.tsx`）

#### B1 通用架构
- **评价**：宿主参数选得很专业——`dpr={[1,1.5]}`、`antialias:false`、`powerPreference:"low-power"`、`depthWrite:false`、`alphaTest:0.01`，星/萤用 `AdditiveBlending`、瓣/叶/雪用 `NormalBlending`。`createNoopEvents()` 关掉指针射线，注释说明了它修的是 AnimatePresence 快装快卸下 `connect(divRef)` 拿到 null 的崩溃 —— 定位准确、修法正确。6 种模式的参数（数量/尺寸/落速/摆幅/闪烁）都经过调，`stars-frontier` 用 `mainStarRatio` 做主星/散星分层是聪明的低成本手法。
- **不足（三个量化问题）**：
  1. **生成域与视锥不匹配**。相机 `z=5`、`fov=50` → 可见半高 `tan(25°)·d = 0.466d`，`z` 在 ±2.5 随机 → 半高 **1.17–3.50**（均值 2.33）。而 `stars.yRange = [1.2, 5.2]`：纵向只有约 **28%** 落在画面内。`xSpread = 16`（x∈±8），16:10 视口可见半宽 ≈3.73 → 横向约 **47%**。两者相乘：**280 颗星里真正可见约 35–40 颗**，其余全程在视锥外被逐帧计算。`firefly.yRange = [-4.2,-0.4]` 同理（纵向约 51%）。移动端竖屏（aspect≈0.46）可见半宽仅 ≈1.08 → 横向 13.5%，`countMobile=140` 实际可见 **约 5 颗星**。
  2. **粒子像素尺寸随画布高度线性缩水**。`sizeAttenuation` 开启 → 屏幕直径 ≈ `size × (画布CSS高/2) / d`。`stars.size=0.028` 在 900px Hero 里 ≈ **2.5px**（合理）；在 208px 卡片里 ≈ **0.58px**、在 108px 的 `.imagery-bar` 里 ≈ **0.30px** —— **亚像素，等于看不见的噪点**。`snow`(0.02) 更小，`firefly`(0.055) 在卡片里 1.1px。卡片 hover 里的粒子层基本是白付开销。
  3. **数量按 `window.innerWidth` 判定**（`:378`），与容器无关 → 桌面上一个 108px 高的 `.imagery-bar` 也拿 `countDesktop`（星 280×0.7×1.15 ≈ 224 颗 0.3px 的点）。
  4. **每帧 CPU 全量更新双缓冲**：`useFrame` 里 JS 循环遍历全部粒子改 `position` 与 `color`，并对两个 attribute 都置 `needsUpdate = true` → 每帧两次 GPU 上传（最多 ~320 粒子 × 3 float × 2）。这类纯函数式运动（正弦摆/匀速下落/相位闪烁）完全可以放进 vertex shader，CPU 归零。
  5. **`reduce` 只读一次**（`:374`），运行中改系统偏好不生效（次要）。
- **优化**：见 P1-1（按 R3F `viewport` 反算生成域、数量、size）与 P2-4（shader 化）。

#### B2 `stars` / `stars-frontier`
- **评价**：`sway 0.008`、`blink:"soft"`（底亮 0.7 + 幅度 0.35）、`points.rotation.y += delta*0.004` 的极慢天旋——「几乎静止的星空」把握得准，没有做成闪烁圣诞灯。`particle-clip-sky`（`inset(0 0 42% 0)`）把星限制在天空带，物理上正确。
- **不足**：除 B1 通用问题外，`stars-frontier` 被排除在天旋之外（`isStarMode(mode) && mode !== "stars-frontier"`，逻辑等价于 `mode==="stars"`，写法绕）。
- **优化**：条件简化为 `mode === "stars"`。

#### B3 `firefly`
- **评价**：`yRange` 压在下半场避开文字区、`wander` 二维游走 + `blink:"hard"`（`pow(max(0,sin),2.4)` 做出萤火的骤亮骤暗）—— 闪烁曲线的整形是这套粒子里最见功力的一处。
- **不足**：`yRange=[-4.2,-0.4]` 下界 -4.2 远超可见半高，约一半萤火常驻画外；`Math.abs(pos[ix]) > xSpread*0.55` 的回收用 `pos *= 0.98` 缓慢拉回，在边界外会长时间徘徊。
- **优化**：生成域按 `viewport` 计算后，回收改环绕（wrap）而非衰减。

#### B4 `petals` / `leaves` / `snow`
- **评价**：`petals` 与 `leaves` 的差异化做得细（叶更慢 `fall 0.11`、摆幅更大 `sway 0.16`、额外加 z 轴翻滚），`spring` 用 `density 0.65`、`flowers` 用 `1.28` 拉开「春稀/花意密」，`snow` 用 `materialOpacity` 与柔 sprite。
- **不足**：**三者共用同一张圆形柔 sprite**（`createCircleTexture`），且 `PointsMaterial` 无法做单粒子旋转 → 花瓣读作「粉色尘埃」、落叶读作「褐色尘埃」，唯一区分靠颜色和速度。这是三个模式里表现力损失最大的一处。
- **优化**：改 `ShaderMaterial` + 各自 sprite（瓣/叶轮廓），在 vertex/fragment 里按 `phase` 旋转 UV，即可获得「翻转飘落」。

---

### C. 编排 / 交互层

#### C1 Hero 入场编排（`HeroSection.tsx`，`HERO_CHOREO`）
- **评价**：整套项目最出彩的部分。beat 0.9s 为节拍单位，题名 delay .25/dur 1.2，句段 stagger 1.35 且带 `blur 6px → 0`，CTA 等**实测最后一行可见后**才出现，scrollCue 再延 2.15 —— 是有编舞意识的时间设计，不是堆 delay。`sceneY [0,50]` / `textY [0,90]` 双速视差，`.hero-lines-clip.is-overflowing` 用 mask 做溢出淡出而非硬裁。
- **不足**：Hero canvas 滚出视口后仍逐帧渲染（无 `IntersectionObserver` / `frameloop` 门控），长页面滚动全程带着一个满负荷 WebGL。
- **优化**：见 P1-4。

#### C2 卡片 hover 氛围（`PoemCardAtmosphere.tsx`）
- **评价**：想法很好——hover 时「这首诗的意境从卡片里透出来」，与 Hero 同源保证了语义一致；`AnimatePresence` 仅 active 时挂载，静置零成本；`reduce` 与 `particles==="none"` 都短路掉了 canvas。
- **不足**：**这是全项目最集中的缺陷区**，本质是「把全视口场景原样塞进 108–260px 的格子」：
  - `intensity="soft"` 只降 opacity，**不缩几何** → 月亮占卡高 56–78%（A4）、酒焰光核大于卡片（A12）、雨滴行程 1080px（A7）、灯笼 45% 时就飞出卡外（A9）、山脊压成锯齿噪线（A5）、暗角把四角压黑（A16）。
  - 粒子在卡片里 **0.3–1.1px**，且数量仍按桌面基数（B1）。
  - **每次 hover 创建/销毁一个 WebGL 上下文**：`/imagery` 有 35 个 `.imagery-bar`，`ATMOS_EXIT=1.15s` 意味着快速横扫时多个 canvas 同时存活并持续上下文创建 —— 浏览器上下文上限约 16，可触发 `Too many active WebGL contexts, oldest context will be lost`。且为一个 hover 装饰把 three.js 拉进了首屏之外的运行时。
  - `bg-ink/25` scrim 叠在已经很暗的场景上，hover 后卡片整体变暗而非「透出意境」。
- **优化**：见 P1-2、P1-3（需要一次方案选择，已在第四章标注推荐）。

#### C3 意境带 / 六道入口两速过渡（`ImageryBar.tsx` · `HomeImageryGates.tsx` · `globals.css` `.imagery-bar`/`.imagery-band__gate`）
- **评价**：「入场利落 0.45s / 离场慢淡 1.15s」的两速设计是对的——离场慢让鼠标扫过时不会闪成频闪灯。CSS 侧用默认 1.15s + `.is-active` 覆盖 0.55s 实现，写法干净。`useHoverFocusActive` 把 hover ∪ focus 合成 active，键盘可达性到位。
- **不足**：**三处组件的时长不一致，且注释撒谎**。`ImageryBar.tsx:33` 与 `HomeImageryGates.tsx:139` 都写「hover 动效与诗卷卡片一致」，但 `PoemCard.tsx` 用的是 `PoemCardAtmosphere` 默认值 **0.45/0.45**，壳体过渡 700ms；意境带是 **0.45/1.15** + 壳体 1.15s。同一站点三种离场节奏。
- **优化**：把 `ATMOS_ENTER/ATMOS_EXIT` 提取为单一常量源（`PoemCardAtmosphere.tsx` 导出或新建 `lib/atmosphere-timing.ts`），三处引用；`PoemCard` 壳体 `duration-700` 对齐；修正注释。

#### C4 `ScrollReveal` / `PageTransition` / `Banxin`
- **评价**：`ScrollReveal`（opacity + y28，0.9s，`margin -8%`）与 `PageTransition`（0.7s 淡入）都克制且统一用 `--ease-elegant` 的 `[0.22,1,0.36,1]`。`Banxin` 的「落版」序列（界线→水洗→卷号→鱼尾→页码→翻卷 ≈1s）+ 方向性 folio roll 是有文化考据的原创装置，`README.md:204` 还明确写了它的使用边界（只给三根线，不满站铺）—— 这种自我约束在装饰系统里很少见。
- **不足**：无实质缺陷。`Banxin` 与意境系统正交，不参与本次改动。

---

## 三、系统性缺陷汇总（按类分级，附证据）

### D. 正确性 bug（4 项 + 1 矛盾）
| # | 问题 | 证据 |
|---|---|---|
| D1 | reduced-motion 下 `.lantern-rise` / `.birds-bob` / `.rain-drop` / `.ripple-ring` 被 `opacity:0 !important` 整体隐藏。`festival` 因此退化为纯渐变（`mist:false`+`particles:"none"`），所有禽鸟消失。同文件 `.wine-ember` 的「停动画、保留静态」才是正确范式 | `globals.css:736-750` · `theme-map.ts:311-319` |
| D2 | mount 后随机采样导致月/日/灯笼/鸟/舟**可见跳变**，且同一首诗每次访问构图不同 | `InkBackground.tsx:350-363` · `CelestialBodies.tsx:385-403` |
| D3 | 涟漪三环不同心 + 原地脉动而非外扩 | `InkBackground.tsx:988-1000` · `globals.css:706-716` |
| D4 | `transition-[background] duration-1000` 永不生效（gradient 不可插值 + theme 不原地变） | `InkBackground.tsx:389` |
| D5 | 寒江同时有静态 CSS 雪点与 WebGL 飘雪，违反 `README.md:79` 明文约定 | `theme-map.ts:171-178` · `InkBackground.tsx:827-851` |
| D6 | `.ink-mist-a` 被两个元素共用，同 36s 同相位 → 同步摆动 | `globals.css:326` · `InkBackground.tsx:696,726` |

### E. 尺度失配（根因一处，症状七处）
`intensity="soft"` 只缩 opacity 不缩几何（`InkBackground.tsx:334,344`）。症状：月亮占卡高 56–78%、酒焰大于卡片、雨滴行程 1080px、灯笼 45% 飞出卡外、暗角压黑四角、山脊成噪线、粒子 0.3–1.1px。所有视口单位（`vh`/`vw`）在卡片语境下都是错的单位。

### F. 性能
| # | 问题 | 量级 |
|---|---|---|
| F1 | 粒子生成域超出视锥，大量粒子全程画外仍逐帧计算 | 星 280 颗中约 240 颗无效；移动端竖屏可见约 5 颗 |
| F2 | 每帧 JS 遍历 + 双 attribute 上传 | 最多 ~320 粒子 × 2 buffer/帧 |
| F3 | 卡片 hover 反复创建/销毁 WebGL 上下文 | `/imagery` 35 格；上限约 16 上下文 |
| F4 | Hero canvas 滚出视口后不停帧 | 全页滚动期间满负荷 |
| F5 | 常驻 `will-change` 约 70 个元素（58 雨滴 + 8–11 灯笼 + 雾/云/酒焰） | 每个 = 一个常驻合成层 |
| F6 | 全屏模糊层堆叠（~15 层 blur + 3 层山 SVG filter + `mix-blend-mode: overlay` 纸纹） | 移动端 fill-rate 敏感 |

### G. 无障碍
- D1（reduced-motion 下主题消失）。
- 正面：所有场景容器 `aria-hidden`、`pointer-events-none`；`useHoverFocusActive` 支持 focus；`ParticleBackground` 在 reduce 下完全不挂载 canvas。这部分基础是合格的。

### H. 一致性 / 表现力
- H1：三处 hover 时长不一致且注释与实现相反（C3）。
- H2：**22 主题中 10 个 `particles:"none"`**，其中 `parting`/`landscape`/`bamboo`/`reclusion`/`mountain` 等在扣掉静态装置后**只剩雾在飘**，主题间运动辨识度不均。`parting`（离别）是首页六道入口之一，hover 后几乎无运动回报。
- H3：鸟不扇翅（A10）；花瓣/落叶/雪共用圆 sprite（B4）；雨的注释与实现不符（A7）。
- H4：移动端遮罩双重收缩——`.particle-safe-center`（椭圆 48%×42% 透明）+ `.particle-clip-sky`（`inset(0 0 42% 0)`）在窄屏几乎不剩可见天空带。

---

## 四、优化方案（分级）

> 建议按批次实施，每批可独立验证。**P0 是零视觉风险的 bug 修复，建议先做**；P1 中 `P1-2/P1-3` 涉及一次设计取舍，已标注推荐方案。

### P0 · 正确性（改动小、收益确定、无设计争议）

**P0-1 删除死 transition** — `InkBackground.tsx:389` 去掉 `transition-[background] duration-1000`。

**P0-2 随机采样改确定性种子**（消除 mount 跳变 + 让同一首诗构图恒定）
- 新建 `lib/scene-seed.ts`：字符串 hash（如 cyrb53）→ `mulberry32` 伪随机生成器，导出 `makeRng(seed: string)`。
- `InkBackground` 增加 `seed?: string` prop；把 `sampleBirdLayout` / `sampleLanterns` / `boatLeft` 的三个 `useEffect`（`:350-363`）改为 `useMemo(() => …, [seed, visual])`，**删除 useEffect 与默认占位值**（SSR 与 CSR 同结果，无 hydration 风险）。
- `CelestialBodies` 增加 `seed` prop，`sampleMoonJitter` / `sampleSunPlacement` 同样改 `useMemo`（`:385-403`），删除 `midPlacement` 首帧兜底与死参数 `theme`/`void _theme`。
- 调用方传 seed：`HeroSection` / `ThemeScene` 传 `poem.id`；`PoemCardAtmosphere` 增加 `seed` 透传，`PoemCard` 传 `poem.id`，`ImageryBar`/`HomeImageryGates` 传 `tag`。

**P0-3 修涟漪** — `InkBackground.tsx:988-1000` 三环改共享中心（统一 `top` 基线 + `translate(-50%,-50%)`，或用 `aspect-ratio` 让 height 由 width 推出）；`globals.css:706-716` `ripplePulse` 改单向外扩（`scale .85→1.55`、`opacity .45→0`），三环 delay 各差 1/3 周期。

**P0-4 寒江去 CSS 雪点** — `lib/theme-map.ts:171` 的 `snow-river` 改 `snow: false`（保留 `winter`）。对齐 `README.md:79`。

**P0-5 reduced-motion 保留静态形态**（照 `.wine-ember` 范式）— `globals.css:736-742`：
- `.lantern-rise` → `animation: none`，`opacity: .5`，保留一个静态 `transform` 位置（灯笼静静挂着）。
- `.birds-bob` → 只 `animation: none`，**不清 opacity**。
- `.rain-drop` / `.ripple-ring` 可继续隐藏（细线/圆环静止确实是残留物），但 `.rain-veil` 已保留空蒙罩，`rain` 主题不会空；`festival` 修完后也不再空。

**P0-6 雾去同相位** — `globals.css` 增 `.ink-mist-c { animation: mistDrift 41s ease-in-out infinite; animation-delay: -13s; }`，`InkBackground.tsx:726` 换用它。顺带给 `.ink-cloud-b` 加 `animation-delay: -18s`。

**P0-7 暗角随强度缩放** — `InkBackground.tsx:1114` 的 `rgba(0,0,0,0.55)` 改为随 `atmScale` 计算（卡片下约 0.3）。

### P1 · 尺度与性能

**P1-1 粒子按容器反算**（`ParticleBackground.tsx`）
- 在 `Particles` 内用 `useThree(s => s.viewport)` 取 z=0 平面的世界宽高，生成域改由它推出：`xSpread = viewport.width * 1.15`；`yRange` 改为「相对上/下带的比例」（如 stars = 顶部 45% 带、firefly = 底部 40% 带）而非绝对世界坐标。回收/环绕边界同步改用 viewport。
- 数量改按**容器面积**：`useThree(s => s.size)` 拿画布 CSS 尺寸，`count = round(base × density × clamp(area / refArea, 0.15, 1))`，`refArea` 取 1440×900。取代 `window.innerWidth < 768`（`:378`）。
- 尺寸改按**目标像素**反算：`size = targetPx * 2 * d / canvasHeightCss`（`d≈5`），让星在任何画布里都稳定落在 2–3px；或保留 `sizeAttenuation:false` 直接用像素尺寸。
- 附带：`isStarMode(mode) && mode !== "stars-frontier"` 简化为 `mode === "stars"`；`reduce` 改用 `matchMedia` 的 `change` 监听。

**P1-2 卡片场景独立尺度**（`InkBackground.tsx` + `globals.css`）
- `InkBackground` 的 `intensity` 扩为 `"full" | "soft" | "card"`（或加 `scope` prop）。`card` 下：
  - 卡片容器（`.imagery-bar` / `.imagery-band__gate` / PoemCard 壳）加 `container-type: size`；把 `--rain-fall`、灯笼关键帧位移、酒焰尺寸、月亮 `sizePx` 的 `vh/vw` 全部换成 `cqh/cqw`（关键帧里的 `-54vh` → `-54cqh` 等），一次性解决 A7/A9/A12/A4 的行程与尺寸。
  - 只渲染该主题的**签名装置 + 底色 + 一层雾**，关掉三层山（只留远景或全关）、`footHaze`、`.ink-mist-sheet`、CSS 雪点。
  - 去掉 `PoemCardAtmosphere` 里的 `bg-ink/25` 或降到 `/10`，配合 P0-7 让 hover 是「透出」而非「压暗」。

**P1-3 卡片 hover 不开 WebGL**（推荐；替代方案见下）
- 推荐：`PoemCardAtmosphere` 不再挂 `ParticleBackground`。卡片尺度下粒子本就是 0.3–1.1px 的亚像素噪点（B1），删掉**没有视觉损失**，同时消灭 F3 的上下文churn 与 three.js 运行时开销。若需要「有东西在动」，用 CSS 版轻量粒子（3–5 个 `radial-gradient` 点 + `cqh` 位移关键帧）。
- 若坚持保留 WebGL：至少（a）`exit` 期间立即卸载 canvas，只让 CSS 层淡出；（b）全局单例——同一时刻只允许一个卡片持有 canvas（用一个 module-level 的 active id）。

**P1-4 Hero canvas 视口门控** — `ParticleBackground` 增加 `IntersectionObserver`，不可见时 `frameloop="never"`（或直接不渲染 `<Canvas>`）；`HeroSection` 已有 `useScroll`，也可用 `scrollYProgress` 阈值驱动。

**P1-5 收敛 `will-change`** — 去掉 `.rain-drop`（`globals.css:646`）与 `.lantern-rise`（`:586`）上的常驻 `will-change`（transform/opacity 动画本身已触发合成，无需提示）；`.ink-mist`/`.ink-cloud`/`.wine-ember` 数量少可保留。

**P1-6 移动端遮罩自适应** — `.particle-safe-center`（`:377`）与 `.particle-clip-sky`（`:371`）的椭圆/inset 尺寸按视口比例调整（窄屏媒体查询里把 safe-center 缩到 38%×30%、clip-sky 放宽到 `inset(0 0 30% 0)`），保证竖屏仍有可见天空带。

### P2 · 表现力与一致性

**P2-1 补齐静态主题的签名运动**（对应 H2，逐一低成本）：
`parting` 渡口水痕（复用 `ripple-ring` 单环 + 极慢）· `bamboo` 竿身 ±0.4° 慢摆（12s，叶片错相位）· `landscape`/`mountain` 云影掠过山脊（一层极慢 `translateX` 的暗带）· `reclusion` 柴烟/雾涨落（雾层 opacity 缓变）· `river` 水面波光（水平细带 `opacity` 呼吸）。每项 = 1 个 keyframe，遵守「一主一辅」。

**P2-2 鸟扇翅** — 翅膀两组 path `opacity` 交替，0.6s 周期，各鸟 `animation-delay` 错开。

**P2-3 雨改程序化** — 58 条硬编码元组（`InkBackground.tsx:740+`）改由 P0-2 的种子生成器按容器宽度分布；中央稀疏带与 `.rain-field` mask 二选一（建议留 mask，去掉硬编码稀疏带）。

**P2-4 粒子 shader 化** — `PointsMaterial` → `ShaderMaterial`，位移/闪烁在 vertex shader 里按 `uTime` + 每粒子 `phase/speed` attribute 计算，`useFrame` 只更新一个 uniform：消除 F2 全部 CPU 开销，并可给 `petals`/`leaves` 做单粒子旋转与各自 sprite（解决 B4「粉色尘埃」问题）。

**P2-5 统一 hover 时长** — 新建 `lib/atmosphere-timing.ts` 导出 `ATMOS_ENTER = 0.45` / `ATMOS_EXIT = 1.15`；`PoemCardAtmosphere` 默认值改用它，`ImageryBar` / `HomeImageryGates` 删除各自的本地常量；`PoemCard.tsx:92` 壳体 `duration-700` → 与 1.15s 对齐（或统一改用 `.is-active` 两速 CSS）；修正 `ImageryBar.tsx:33` 与 `HomeImageryGates.tsx:139` 的注释。

**P2-6 清理** — 删 `CelestialBodies` 的 `theme`/`void _theme`（随 P0-2 一起）。

---

## 五、验证

**改动前先建立基线**（同一组截图/录屏，改动后逐张对比）。

1. **逐主题视觉核对**：`npm run dev`，用 `theme` 反查各主题的若干 `poem.id`，逐个访问 `/poem/[id]`，对照 `README.md:76-79` 的签名原则核验——每主题是否只有一个主运动、有无「光斑+粒子」双叠、寒江是否已无静止雪点、华灯是否只有灯笼。
2. **mount 稳定性**（验 P0-2）：连续刷新首页与详情页 5 次，观察月/日/灯笼/鸟/舟**是否仍有跳变**；同一 `poem.id` 反复进入，构图必须完全一致。
3. **卡片尺度**（验 P1-2）：hover 首页六道、`/imagery` 的 `.imagery-bar`（108px，最极端）、`/poems` 诗卷卡，检查月亮是否不再糊满、灯笼是否在卡内完成升起、四角是否不再压黑。
4. **WebGL 上下文**（验 P1-3/F3）：在 `/imagery` 快速横扫 35 个格子，控制台确认无 `Too many active WebGL contexts` / `context lost`；DevTools Memory 快照对比 canvas 数量。
5. **性能**：DevTools Performance 录 Hero 停留 5s + 滚动 5s，对比 Composite/Raster 时间与 dropped frames；Rendering 面板开 Layer borders 数常驻合成层（验 P1-5 前后差异）；滚过 Hero 后确认 GPU 占用回落（验 P1-4）。
6. **粒子可见量**（验 P1-1）：1440×900 与 390×844 两档，截图放大量粒子像素直径（目标 2–3px）与画面内可见颗数；确认移动端不再是「约 5 颗星」。
7. **reduced-motion**（验 P0-5）：系统开启「减少动态效果」，逐一检查 `festival`（必须仍有静态灯笼）、有鸟的主题（鸟必须仍在）、`wine`（静态暖晕，作为对照基准），并确认无任何位移动画。
8. **回归**：`npm run lint` + `npm run build`（P0-2 涉及 SSR/CSR 一致性，构建期与首屏 hydration 必须无警告）。

> 注：涉及 `node_modules/next/dist/docs/` 的内容仅在需要改动 Next.js 侧行为时查阅；本方案全部改动落在组件、CSS 与 `lib/`，不触及路由/渲染约定。
